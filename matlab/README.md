## MATLAB Simulations

Here are the MATLAB simulations used in the **PoLiNoM** project:

- **coronaSimulation**: generates a `.wav` audio file containing the corona noise.
- **FilterCoronaAligned**: simulates the adaptive filter algorithm in a realistic noise cancellation context.

Both scripts were used to validate the operation of the ANC system.
